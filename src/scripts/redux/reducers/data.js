

const defaultState = {
    city:'恩施',
    count:2020
}

export const data = (state=defaultState,actions)=>{
    switch(actions.type){




        default:
        return state;
        break;
    }
}